# 变更记录

## 未发布

- 前端独立 CI 中，方案设计文档测试不再要求检出兄弟后端仓库。
- 快速开始和 Compose 安装指向公开的 `pms-distribution` 仓库，并使用公开发布的 GitHub 克隆地址。
- 增加结构化的项目与权限审计追溯：可按项目 / 结果 / 请求筛选，字段级详情抽屉，脱敏前后快照，保留任务审计，以及 V14 审计上下文迁移。
- 统一生命周期状态颜色：未开始 / 待办为中性灰，进行中为橙色，已完成为成功绿，已终止为危险红，已删除为弱灰。项目、任务、节点和里程碑标签共用这套映射。
- 为应用内使用手册每个模块补充按角色的示例，用日常页面动作解释人员读取和组织读取，并修正内置角色矩阵，使其与默认权限种子一致。
- 刷新失败时保留已加载的工作台卡片；节点仍有未完成任务时禁止完成该节点。
- 重写应用内业务规则和设计规范文案，使其与当前行为一致：邮箱搜索范围、完整邮箱回退姓名、没有管理员重新启用接口、中间业务线、导入模板字段、审计范围、节点完成，以及实际壳层几何。
- Plane + Taiga 视觉迁移（2026-09-03）：统一令牌和应用壳层，完成项目列表 / 详情、工作台、治理页面、组织画布、反馈中心与文档中心的工作台式视觉层级；只调整视觉、响应式和可访问性，不改变业务接口或状态机。跨页面验收覆盖 `1440 × 900` 与 `390 × 844`，`pnpm test` 130/130、类型检查、构建和隐私扫描通过。
- 发布收口快照（2026-09-01）：`pnpm test` 111/111 通过；`pnpm typecheck` 与 `pnpm build` 通过。Playwright 桌面、390px 窄屏和人工录制流程在固定 `zh-CN` 语言下通过。
- 工作台改为请求 `GET /workbench`，不再分别拉取项目、任务和评论。
- 可从看板或工作台打开任务详情，查看子任务、评论和附件。
- 增加顶栏搜索（项目、任务、里程碑、评论）和站内通知铃。项目评论会通知关注人；完成或回退节点会打开对应阶段。
- 本地贡献者启动路径指向兄弟仓库的 MySQL 8。
- 壳层、登录页和工作台支持中英文切换，偏好保存在 localStorage。
- 语言覆盖扩展到项目列表、项目详情、任务看板、任务详情、协作页签、管理后台和使用手册框架。状态和优先级标签跟随当前语言。
- 后端启用对应提供方时，登录页显示 OIDC / LDAP 按钮。
- 更新 README，记录工作台、搜索、通知、语言切换和 SSO 回调。Kubernetes 安装仍在后端 Helm chart。
- 协作区增加项目甘特图和日历视图，读取已有项目 / 节点日期、里程碑截止日期和带日期的任务。
- 刷新应用内使用手册和 Markdown 手册，使其匹配搜索、通知、甘特图和管理后台行为。登录截图已更新；其余截图将跟随线上界面。
- 区分登录连接失败与凭据错误：后端不可达时提示服务不可用，而不再使用笼统的稍后重试。

## 2026-09-02

- 生产就绪第一阶段本地验收：`pnpm test` 112/112、`pnpm typecheck`、`pnpm build`、桌面流程、390px 窄屏流程和人工录制，均在本机 MySQL 应用上通过。

## 1.0.0

- Vue 3 工作台、项目看板、组织画布和管理后台。
- CI 增加隐私黑名单扫描。演示文案只使用虚构身份。
